services:
  mock-api:
    build: .
    ports:
      - "3000:3000"
    environment:
      PORT: 3000
      SCHEMA_PATH: /app/schema.json
      RESPONSE_DELAY_MS: ${RESPONSE_DELAY_MS:-0}
    volumes:
      - ./schema.json:/app/schema.json:ro
